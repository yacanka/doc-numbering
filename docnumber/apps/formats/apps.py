from django.apps import AppConfig


class FormatsConfig(AppConfig):
    """Application configuration for document format definitions."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.formats'
