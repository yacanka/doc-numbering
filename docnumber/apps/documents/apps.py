from django.apps import AppConfig


class DocumentsConfig(AppConfig):
    """Application configuration for generated document records."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.documents'
